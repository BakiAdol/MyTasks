@model MyTasks.Models.SearchModelMembers

<partial name="_navBar" />

<partial name="_searchBox"/>

<div class="searchResultShowContainer">
    @{
        foreach (var task in Model.TaskModel)
        {
            @Html.Partial("_taskCard",task)
        }
    }
</div>